import { createTheme } from '@material-ui/core'

const theme = createTheme({
  palette: {
    primary: {
      main: '#00bcd4',
      contrastText: '#fff'
    },
    secondary: {
      main: '#ff9800'
    }
  }
})

export default theme
